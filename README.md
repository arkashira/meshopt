# Meshopt Service Mesh
Meshopt is a basic service mesh implementation with monitoring capabilities.

## Local Deployment
1. Run the service mesh: `python src/meshopt.py --monitoring-port 8080`
2. Access the monitoring dashboard: `http://localhost:8080`

## Configuration
Create a configuration file `config.json` with the following format:
