# MeshOpt

MeshOpt is a safety-first auto-apply engine for mesh optimizations.

## Usage

1. Define an off-peak window using the `--start-time` and `--end-time` arguments.
2. Add optimizations using the `--spec` argument.
3. Run the `meshopt` command to auto-apply optimizations.

## Example
